---
import Layout from "./Layout.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <nav class="bg-slate-600 text-white py-4">
    <ul class="container mx-auto px-4 font-semibold">
      <li>
        <a href="/">К списку</a>
      </li>
    </ul>
  </nav>
  <slot />
</Layout>
